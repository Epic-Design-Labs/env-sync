{
  "name": "@epicdesignlabs/env-sync",
  "version": "1.0.0",
  "description": "Render .env files from 1Password, matching variables to fields by name.",
  "type": "module",
  "bin": { "env-sync": "bin/env-sync.js" },
  "files": ["bin", "src"],
  "engines": { "node": ">=20" },
  "scripts": { "test": "node --test test/*.test.js" },
  "repository": "github:Epic-Design-Labs/env-sync",
  "license": "UNLICENSED"
}
